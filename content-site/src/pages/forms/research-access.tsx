import type {ReactNode} from 'react';
import {FormStubPage} from '@site/src/components/FormStubPage';

export default function ResearchAccessForm(): ReactNode {
  return (
    <FormStubPage
      title="Request full research access"
      summary="Reserved form route for analyst and research access requests."
    />
  );
}
